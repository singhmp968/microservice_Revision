server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/GQlPractice
    username: root
    password: 'root'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate.enable_lazy_load_no_trans: true
      hibernate.id.new_generator_mappings: false
    hibernate:
      ddl-auto: update
    show-sql: true

#  graphql:
#    servlet:
#      mapping: /graphql
#    graphiql:
#      enabled: true
#      endpoint: /graphql
#      path: /graphiql